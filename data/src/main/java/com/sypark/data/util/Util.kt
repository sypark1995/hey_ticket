package com.sypark.data.util

object Util {
    enum class ButtonType {
        DAY, WEEK, MONTH
    }

    enum class NewButtonType {
        CREATED_DATE, VIEWS
    }

    enum class SearchType {
        PERFORMANCE, ARTIST
    }
}